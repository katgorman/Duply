FIRESTORE_PRODUCTS_COLLECTION=beauty_products
FIRESTORE_WEB_CACHE_COLLECTION=web_query_cache
FIRESTORE_CACHE_TTL_SECONDS=900
FIRESTORE_SEARCH_CACHE_TTL_SECONDS=300
FIRESTORE_WEB_CACHE_TTL_SECONDS=604800

# Recommendation engine mode:
# auto = try local model, fall back if unavailable
# disabled = always use metadata-only fallback ranking
# DUPLY_MODEL_MODE=disabled

# DataForSEO-backed catalog ingestion and price matching.
# Firestore should be the primary searchable catalog. Use:
# `python backend/augment_catalog.py`
# or POST `/admin/augment-top-brands`
# to ingest top-brand live products into Firestore once.
# For long-running hosted ingestion/cleanup, prefer the resumable admin job flow:
# POST `/admin/jobs` with kind `cleanup-catalog`, `augment-us-retailers`, or `augment-top-brands`
# then POST `/admin/jobs/{jobId}/run` with a small `maxSteps`.
# DATAFORSEO_LOGIN=your-dataforseo-login
# DATAFORSEO_PASSWORD=your-dataforseo-password
# DATAFORSEO_BASE_URL=https://api.dataforseo.com
# DATAFORSEO_LOCATION_CODE=2840
# DATAFORSEO_LANGUAGE_CODE=en
# DATAFORSEO_DEVICE=desktop
# DATAFORSEO_OS=windows
# DATAFORSEO_TASK_TIMEOUT_SECONDS=90
# DATAFORSEO_POLL_INTERVAL_SECONDS=2
# DUPLY_WEB_SEARCH_MAX_RESULTS=8
# DUPLY_WEB_SEARCH_CACHE_TTL_SECONDS=3600
# DUPLY_WEB_PRODUCT_INFO_CACHE_TTL_SECONDS=86400
# DUPLY_CACHE_SCHEMA_VERSION=us-retailers-v3
# DUPLY_OFFICIAL_RETAILER_TIMEOUT_SECONDS=12
# DUPLY_OFFICIAL_RETAILER_MAX_WORKERS=6
# DUPLY_LIVE_LIMIT_PER_BRAND=6
# DUPLY_LIVE_CATEGORY_MAX_BRANDS=19
# Optional image lookup for catalog products with missing images.
# First tries the product source page for real Open Graph/Twitter images.
# Set false to disable source-page image fetching.
# DUPLY_SOURCE_IMAGE_LOOKUP_ENABLED=true
# DUPLY_WEB_IMAGE_CACHE_TTL_SECONDS=86400

# Choose one Firebase credential method for hosted deployments.

# Option 1: Path to a service account JSON file on the server.
# GOOGLE_APPLICATION_CREDENTIALS=/etc/secrets/firebase-service-account.json

# Option 2: Inline the entire service account JSON as one secret env var.
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"your-project-id","private_key_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n","client_email":"firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com","client_id":"...","auth_uri":"https://accounts.google.com/o/oauth2/auth","token_uri":"https://oauth2.googleapis.com/token","auth_provider_x509_cert_url":"https://www.googleapis.com/oauth2/v1/certs","client_x509_cert_url":"https://www.googleapis.com/robot/v1/metadata/x509/..."}

# Option 3: Split the JSON into separate env vars.
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project-id.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\\n...\\n-----END PRIVATE KEY-----\\n"
